#labels Phase-Support
= FAQ =

TODO

which files should be _configed_
 * web.xml
 this is the java web container rule, we can not avoid this configuration, bala, bala ...

 * jmap-config.properties
 this config file just need only one line for the package prefix name
